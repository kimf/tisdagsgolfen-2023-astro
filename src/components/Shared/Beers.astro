---
export interface Props {
  beers: number;
}

const { beers } = Astro.props;
---

{
  beers > 0 && (
    <div style="font-size: 0.8em;">
      <span>{beers > 1 ? `${beers} x 🍺` : '🍺'}</span>
    </div>
  )
}
